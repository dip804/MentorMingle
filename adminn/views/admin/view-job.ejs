<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Available Jobs</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-10">

    <h2 class="text-2xl font-bold text-center mb-6">Available Jobs</h2>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        <% jobs.forEach(job => { %>
            <div class="bg-white p-6 rounded-lg shadow-md border border-gray-200">
                <h3 class="text-xl font-bold mb-2"><%= job.title %></h3>
                <p class="text-gray-500 mb-2"><%= job.company %></p>

                <span class="bg-gray-200 text-gray-700 px-3 py-1 rounded-full text-sm">
                    <%= job.type %>
                </span>

                <p class="text-gray-600 mt-3"><%= job.description %></p>

                <a href="<%= job.url %>" target="_blank"
                   class="block mt-4 bg-teal-500 text-white text-center py-2 rounded-lg hover:bg-teal-600">
                    Apply Now
                </a>
            </div>
        <% }); %>
    </div>

</body>
</html>
